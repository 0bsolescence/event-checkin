<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- DayNight parent: system light/dark handled by Material3; only the
         brand tokens from colors.xml are pinned. -->
    <style name="Theme.EventCheckIn" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/brand_on_primary</item>
    </style>
</resources>
